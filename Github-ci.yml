name: Java CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        with:
          java-version: '8'
          distribution: 'adopt'
      - name: Build with Maven
        run: mvn --clean install